#include "../include/timer.hpp"
#include "../include/queryParser.hpp"

int main() {

    newLine();
    queryInput();

    // for (int i = 0; i < tokens.size(); i++) {
    //     std::cout << tokens[i] << '\n';
    // }
    timer.stop();
    return 0;
}